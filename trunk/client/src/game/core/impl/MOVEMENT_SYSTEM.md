# MovementSystem - War3 风格移动系统

参考魔兽争霸3的移动机制实现的完整移动系统。

## 📋 功能特性

### 1. A* 寻路算法
- 基于地图格子的 A* 寻路
- 支持 8 方向移动（上下左右 + 4个对角线）
- 自动避开阻挡区域
- 使用曼哈顿距离启发式函数
- 边界检查和目标可达性检测

### 2. 路径平滑
- 自动优化生成的路径
- 使用视线检测（Line of Sight）进行路径简化
- 移除不必要的路径点
- 生成更自然流畅的移动轨迹

### 3. 平滑转向系统
- 渐进式旋转，而非瞬间转向
- 可配置转向速度（度/秒）
- 自动计算最短旋转路径
- 类似 War3 的转向效果

### 4. 精确到达判定
- 可配置到达半径（arrivalRadius）
- 避免目标点"震荡"问题
- 支持路径点序列跟随
- 自动切换到下一个路径点

### 5. 移动状态管理
```typescript
enum MoveState {
    Idle = 'idle',              // 待机
    MovingStraight = 'moving_straight',  // 直线移动
    Moving = 'moving',          // A* 路径跟随
    Turning = 'turning',        // 转向中
    Blocked = 'blocked',        // 被阻挡
    Arrived = 'arrived',        // 已到达
}
```

## 🧩 模块拆分（更易维护）

为降低复杂度并提升可维护性，移动系统已拆分为独立模块：

- [MovementSystem.ts](src/game/core/impl/MovementSystem.ts)：主协调器（状态机与命令入口）
- [StraightMovementSystem.ts](src/game/core/impl/StraightMovementSystem.ts)：直线移动 + 前方障碍检查
- [PathfindingSystem.ts](src/game/core/impl/PathfindingSystem.ts)：A* 寻路 + 路径平滑
- [PathFollowingSystem.ts](src/game/core/impl/PathFollowingSystem.ts)：沿 A* 路径跟随 + 动态切回直线
- [ObstacleDetection.ts](src/game/core/impl/ObstacleDetection.ts)：DDA 视线检查工具
- [AvoidanceSystem.ts](src/game/core/impl/AvoidanceSystem.ts)：单位避让（分离力 + 接触滑动兜底）

调用关系：`MovementSystem` 根据当前 `MoveState` 分发到直线/路径跟随模块；碰到障碍时调用 `PathfindingSystem` 获取路径。

## 🎮 War3 风格直线运动优化

### 移动策略（完全War3化）

MovementSystem 实现了完整的 War3 风格移动系统：

**第一阶段：直线运动尝试**
```
1. 收到 moveTo() 命令
2. 检查目标点直线路径是否通行
3. 如果通行 → 进入 MovingStraight 状态
4. 直接朝目标运动
```

**第二阶段：每帧动态障碍检测**
```
1. 每帧检查前方 obstacleCheckDistance 是否有新的障碍（默认 3m，可配置）
2. 如果有障碍 → 立即切换到 Moving 状态
3. 自动调用 A* 寻路绕路
```

**第三阶段：路径跟随**
```
1. 沿 A* 路径移动
2. 自动移除不必要的路径点（路径平滑）
3. 直到到达目标
```

### 状态转换图

```
idle
  ↓
moveTo() 命令
  ↓
[检查直线]
  ├─ 通行 → MovingStraight
  │         ↓ (每帧检测)
  │         有障碍 → Moving (A*)
  │                  ↓
  │                已到达 → Arrived
  │
  └─ 不通行 → Moving (A*) 
             ↓
             已到达 → Arrived
```

### 核心优化点

1. **减少寻路计算**: 优先直线走，只在必要时调用A*
2. **响应式障碍检测**: 每帧 3m 预检（可配置），立即反应而不是走到墙
3. **自动路径优化**: 路径平滑移除冗余点
4. **平滑转向**: 逐帧旋转而非瞬间转向

## 🚀 快速开始

### 基础用法

```typescript
import { MovementSystem } from './MovementSystem';

const game = runner.getGame();
const movement = game.getSystem<MovementSystem>('movement');

// 移动单位到目标位置
movement?.moveTo({
    actorId: 'unit_1',
    targetX: 100,
    targetZ: 100,
    speed: 5,  // 5 米/秒
});
```

### 带参数的移动

```typescript
movement?.moveTo({
    actorId: 'unit_1',
    targetX: 200,
    targetZ: 150,
    speed: 8,
    arrivalRadius: 1.0,  // 到达半径 1 米
    turnSpeed: 180,      // 转向速度 180 度/秒
    targetY: 0,          // 目标高度（可选）
});
```

### 检查移动状态

```typescript
// 检查是否在移动
const isMoving = movement.isMoving('unit_1');

// 获取详细状态
const state = movement.getMoveState('unit_1');
// 返回: 'idle' | 'moving_straight' | 'moving' | 'turning' | 'blocked' | 'arrived' | null
```

### 停止移动

```typescript
movement.stopMove('unit_1');
```

## 🎮 与 UnitCommandSystem 集成

MovementSystem 自动与 UnitCommandSystem 集成：

```typescript
const commandSystem = game.getSystem('unitCommand');

// MoveTo 命令
commandSystem.issueCommand('unit_1', {
    type: 'MoveTo',
    targetPos: { x: 100, y: 100 }, // y 对应世界坐标的 z
});

// AttackMove 命令
commandSystem.issueCommand('unit_2', {
    type: 'AttackMove',
    targetPos: { x: 200, y: 150 },
});
```

## 📐 坐标系统

MovementSystem 使用世界坐标系：
- `x`: 水平位置（米）
- `y`: 高度（米）
- `z`: 深度（米）

注意：UnitCommandSystem 中的 `targetPos.y` 对应世界坐标的 `z`。

## ⚙️ 配置参数

### MoveCommand 接口

```typescript
interface MoveCommand {
    actorId: string;        // 单位 ID
    targetX: number;        // 目标 X 坐标（米）
    targetZ: number;        // 目标 Z 坐标（米）
    targetY?: number;       // 目标高度（可选，默认 0）
    speed: number;          // 移动速度（米/秒）
    arrivalRadius?: number; // 到达半径（默认 0.1 米）
    turnSpeed?: number;     // 转向速度（默认 360 度/秒）
}
```

### 系统默认配置

```typescript
private _defaultArrivalRadius = 0.1;  // 默认到达半径（米）
private _defaultTurnSpeed = 360;      // 默认转向速度（度/秒）
private _defaultObstacleCheckDistance = 3; // 前方障碍检测距离（米，可被 UnitConfig 覆盖）
private _pathSmoothRadius = 1.0;      // 路径平滑检测半径
private _collisionRadius = 0.5;       // 碰撞半径（用于RVO）

### 单位级配置（UnitConfig）

支持为不同单位设置不同的障碍检测距离：

```ts
// UnitConfig.ts
export interface UnitConfig {
    // ... 其他配置
    obstacleCheckDistance?: number; // 可选，单位的前方障碍检测距离（米）
}
```

在运行时，`MovementSystem` 会优先读取 `actor.getUnitConfig().obstacleCheckDistance`，若未设置则使用系统默认值（当前为 3m）。
```

## 🏗️ 架构设计

### 系统组成

```
MovementSystem
├── A* 寻路模块
│   ├── 开放列表管理
│   ├── 启发式计算
│   └── 路径重建
├── 路径平滑模块
│   ├── 视线检测
│   └── 路径简化
├── 移动控制模块
│   ├── 直线移动（新）
│   ├── 路径点跟随
│   ├── 速度控制
│   └── 到达判定
└── 转向控制模块
    ├── 角度插值
    └── 最短旋转路径
```

### 数据流

```
UnitCommandSystem
    ↓
MovementSystem.moveTo()
    ↓
[检查直线] → 直线通行
    ↓
MovingStraight 状态 → 每帧检测
    ↓ (有障碍)
A* 寻路 → 生成路径
    ↓
路径平滑 → 优化路径
    ↓
fixedUpdate() → 路径跟随
    ↓
Actor.move() + Actor.setRotation()
```

## 🔄 更新流程

在每个固定帧（fixedUpdate）中：

1. 遍历所有移动中的单位
2. 检查单位状态（活跃、存活）
3. 根据状态执行对应逻辑：
   - `MovingStraight`: 直线移动，每帧检测前方障碍
   - `Moving`: 向当前路径点移动，更新位置和朝向
   - `Turning`: 平滑旋转至目标角度
   - `Blocked`/`Arrived`: 状态机覆盖，等待清理
4. 检查到达条件：
   - 距离路径点 < arrivalRadius
   - 切换到下一个路径点
   - 所有路径点完成 → 状态变为 `Arrived`

## 🆚 与 War3 的对比

| 功能特性 | War3 | MovementSystem | 备注 |
|---------|------|----------------|------|
| A* 寻路 | ✅ | ✅ | 完全实现 |
| 路径平滑 | ✅ | ✅ | 完全实现 |
| 平滑转向 | ✅ | ✅ | 完全实现 |
| 到达判定 | ✅ | ✅ | 完全实现 |
| 8方向移动 | ✅ | ✅ | 完全实现 |
| 直线运动 | ✅ | ✅ | 完全实现 (v1.1) |
| 单位避让 | ✅ | ✅ | War3风格（碰撞检测 + 等待） |
| 编队移动 | ✅ | ❌ | 计划中 |
| Flow Field | ✅ | ❌ | 计划中 |
| 地形高度 | ✅ | ⚠️ | 基础支持 |
| 动态障碍物 | ✅ | ❌ | 计划中 |

## 🐛 已知限制与改进计划

### 当前限制
1. **简单避让策略**: 采用War3风格的碰撞等待，未实现现代RVO算法
2. **无编队支持**: 单位独立移动
3. **静态地图**: 不支持动态障碍物（临时）
4. **平面运动**: 未实现地形高度相关的运动

### 改进方向 (优先级排序)

#### ⭐ 高优先级：现代避让算法 (RVO/ORCA)

**说明**：War3（2002年）并未使用RVO算法（RVO于2008年发表）。War3采用简单的碰撞检测+推挤+等待策略。当前实现已接近War3风格。如需更流畅的避让效果，可考虑实现现代RVO/ORCA算法。

```typescript
interface ModernAvoidanceSystem {
    // 计算避让速度，基于周围单位
    computeAvoidanceVelocity(unit: Actor, neighbors: Actor[], targetVelocity: Vector2): Vector2;
    
    // 获取周围邻居
    getNearbyUnits(unit: Actor, radius: number): Actor[];
    
    // 应用避让到移动系统
    applyRVO(moveData: MoveData, deltaSeconds: number): void;
}
```
**优势**：
- 多单位平滑避让，避免卡顿
- 计算量适中，每单位O(n)（n为邻居数）
- 比War3的简单推挤更流畅
- 适合现代RTS游戏

**War3 vs 现代方案**：
- War3：碰撞检测 → 推挤 → 等待（简单但会卡顿）
- RVO/ORCA：预测碰撞 → 提前避让 → 流畅绕行
- 当前实现：War3风格（碰撞等待），已满足基本需求

**配置（如实现RVO）**

- `avoidanceEnabled: boolean`：是否启用避让算法
- `avoidanceRadiusMultiplier: number`：邻居搜索半径系数
- `rvoTimeHorizon: number`：RVO预测时间窗口
- `maxNeighbors: number`：最大考虑邻居数（性能优化）

#### 编队移动
```typescript
interface Formation {
    leader: Actor;
    followers: Actor[];
    formationType: 'line' | 'wedge' | 'box' | 'circle';
    offset: Vector3[]; // 相对领队的位移
}
```

#### Flow Field 大规模优化
用于100+单位同时移动：
- 生成全局势能场
- 单位跟随梯度向下（朝目标）
- 大幅减少A*调用次数

#### 动态障碍物
- 建筑创建/销毁时更新可通行性
- 临时障碍支持（如技能范围）
- 实时重新寻路判定

## 📚 参考资料

- [War3 Movement System](https://www.codeofhonor.com/blog/the-algorithms-of-warcraft-3)
- [A* Pathfinding](https://www.redblobgames.com/pathfinding/a-star/introduction.html)
- [RVO (Reciprocal Velocity Obstacles)](http://gamma.cs.unc.edu/RVO/) - 单位避让算法
- [Steering Behaviors](https://www.red3d.com/cwr/steer/)

## 📝 版本历史

### v1.1.0 (当前版本 - 最新)
**核心改进：直线运动优化和 Bug 修复**
- ✅ War3 风格直线运动：先尝试直线走到目标，每帧检测前方 3m 障碍（可被 UnitConfig 覆盖）
- ✅ 动态 A* 切换：遇到障碍立即切换 A* 寻路
- ✅ 修复卡住 bug：完整处理 Blocked/Arrived 状态
- ✅ 目标朝向修正：直线运动时朝向最终目标而非临时点
- ✅ 状态转换优化：状态机完整处理所有分支
- ✅ 基础单位避让：War3风格（碰撞检测 + 等待策略）

### v1.0.0 
- ✅ A* 寻路算法
- ✅ 路径平滑（视线检测）
- ✅ 平滑转向
- ✅ 精确到达判定
- ✅ 移动状态管理
- ✅ 与 UnitCommandSystem 集成
- ✅ 地图阻挡检测

## 🔗 相关文件

- [MovementSystem.ts](./MovementSystem.ts) - 主实现文件（597行）
- [Actor.ts](./Actor.ts) - 单位基类（包含 radius 字段）
- [Map.ts](./Map.ts) - 地图和寻路数据
- [UnitCommandSystem.ts](./UnitCommandSystem.ts) - 命令系统集成
- [ModelConfig.ts](../config/ModelConfig.ts) - 配置接口
